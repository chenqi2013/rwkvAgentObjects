# RWKV Agent 项目目录设计上下文

## Direction

参考 RWKV.CN 生态项目页：纯黑底、固定半透明导航、细边框、neutral-900 卡片、冷蓝渐变与克制的辅助文字。信息密度服务于核验，不用大段说明装饰界面。

## Tokens

- 所有页面颜色、边框、焦点、圆角和渐变优先使用 `src/app/globals.css` 语义变量。
- `--primary` 用于蓝色索引、链接和关键状态；`--success`、`--warning`、`--info` 只用于数据状态。
- 品牌标识使用等宽字体，正文优先使用现有 Geist。

## Components

- 优先使用 `src/components/ui` 中的 shadcn 组件。
- 项目卡片使用平面边框与轻微悬停位移，不使用厚重阴影。
- 详情使用 shadcn Sheet；长内容放在 ScrollArea 中。
- 语言切换使用导航栏中的 shadcn Button、语言图标和目标语言短标签；英文显示“中文”，中文显示“EN”。
- 图标按钮必须有可访问名称；外链统一新窗口打开并设置安全 `rel`。

## Interaction

- Agent 项目支持搜索项目名、作者、项目类型和介绍，并按语言与排序过滤。
- `/en/` 与 `/zh/` 使用各自的界面字典、日期格式、metadata 和 `html lang`；语言切换重置当前浏览状态。
- 搜索只匹配当前语言的数据内容，不跨语言产生不可见命中。
- 选择“查看档案”打开详情 Sheet，移动端 Sheet 占满宽度。
- 空字段、空数组和错误状态用紧凑视觉状态表达，不渲染空标题或冗余说明。
