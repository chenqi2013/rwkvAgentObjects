# RWKV Agent 项目目录

这是一个默认英文、可切换中文的只读 RWKV 社区 Agent 项目目录，用于浏览和核验项目档案。

## Product mode

Read / Operate. 用户通过搜索、筛选、排序、语言切换和详情抽屉浏览项目，不在页面内修改原始数据。

## Primary audience

需要维护社区项目清单、检查项目归类和核验项目信息的 RWKV 社区维护者。

## Product truths

- Agent 项目来自根目录 `agent.json`，所有可读内容使用严格的 `{ en, zh }` 成对结构。
- 数据在构建阶段只读加载，部署产物不需要 Node.js 服务器。
- `/` 通过静态入口固定进入 `/en/`，用户可通过导航切换 `/en/` 与 `/zh/`；不根据浏览器语言推断。
- 项目、作者、能力、限制和来源是核心信息。
- 搜索与筛选服务于快速核验；长文本进入详情抽屉，不堆在列表中。
